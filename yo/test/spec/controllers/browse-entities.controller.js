'use strict';

describe('BrowseEntitiesController', function(){
  /*
	beforeEach(function() {
        module(function($provide) {
            $provide.constant('LANG', {});
            $provide.constant('APP_CONFIG', readJSON('test/mock/data/mock-config-multi.json'));
            $provide.constant('SMARTCLIENTPING', {ping: 'offline'});
        });
    });

	beforeEach(module('angularApp'));
	
	var $controller;
	beforeEach(inject(function(_$controller_){
		$controller = _$controller_;
	}));

	it('should have gridOptions.onRegisterApi defined', function(){

		var $scope = {};

      	$controller('BrowseEntitiesController', {
      		'$scope': $scope,
      		'$stateParams': {
      			facilityName: 'dls'
      		}
      	});

      	expect($scope.gridOptions.onRegisterApi).toBeDefined();

	});
  */

});


